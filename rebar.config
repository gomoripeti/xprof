{ct_opts, [{ct_hooks, [cth_readable_failonly, cth_readable_shell]}]}.

{edoc_opts, [{sort_functions, false},
             {pretty_printer, erl_pp},
             {preprocess, true}
            ]}.

{cover_excl_mods, [test_module]}.

{shell, [{apps, [sync, xprof]}]}.

{profiles,
 [{test,
   [{deps, [ {cth_readable, "1.2.1"}, {meck, "0.8.9"} ]},
    {plugins, [ {coveralls, "1.3.0"} ]}
   ]},
  {dev,
   [{deps, [ {sync, "0.1.3"} ]}
   ]},
  {docs,
   [{plugins, [ {rebar3_edoc_chunks, {git, "https://github.com/erszcz/edoc.git", {branch, "import-docs-chunks"}}} ]},
    {provider_hooks, [ {post, [{compile, {edoc_chunks, compile}}]} ]}
   ]},
  {publish,
   [{plugins, [ {rebar3_hex, {git, "https://github.com/gomoripeti/rebar3_hex.git", {branch, "deps"}}} ]},
    {overrides, [ {add, xprof, [{post_hooks, [{edoc, "make -C ../.. override_edoc"}]}
                               ]}
                ]}
   ]}
 ]}.

{cover_enabled, true}.
{cover_export_enabled, true}.

{coveralls_coverdata, [ "_build/test/cover/eunit.coverdata", "_build/test/cover/ct.coverdata" ]}.
{coveralls_service_name, "travis-ci"}.
